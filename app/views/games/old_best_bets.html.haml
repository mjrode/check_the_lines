-# .card.card-block
-#   %h4.card-title.center Past Picks
-#   %p.card-text.center Enter Strength number to filter by game strength
-#   = form_tag old_best_bets_games_path, :method => 'get' do
-#     = number_field_tag :search, params[:search], placeholder: "Strength", class: "form-control center form-width-25 border-black center"
-#     = submit_tag "Filter", :name => nil, class: "btn strength-button center"
%ul.list-group
  %li.list-group-item
    %h1.secondary-font.center
      Correct: #{Game.all_correct_spread_best_bets.where.not("strength > ?", 10000).count}
      Incorrect: #{Game.all_incorrect_spread_best_bets.where.not("strength > ?", 10000).count}

  %h1.clear.center.big-font.padding-top-5#all Best Bet History
  -Game.all_best_bets.where(game_over: true).sort_by{|game| game.strength }.reverse.each do |game|
    =render "bet_display", game: game

  -#%li.list-group-item
  -#  %h2.seconday-font.center
  -#    = link_to " NCAAF Spread Record", "#cb"
  -#  %h1.secondary-font.center
  -#    Correct: #{@old_games.where(sport: "cb").where(correct_prediction: true).count}
  -#    Incorrect: #{@old_games.where(sport: "cb").where(correct_prediction: false).count}
  -#
  -#%li.list-group-item
  -#  %h2.seconday-font.center
  -#    = link_to " NFL Spread Record", "#nfl"
  -#  %h1.secondary-font.center
  -#    Correct: #{@old_games.where(sport: "nfl").where(correct_prediction: true).count}
  -#    Incorrect: #{@old_games.where(sport: "nfl").where(correct_prediction: false).count}
  -#
  -#%li.list-group-item
  -#  %h2.seconday-font.center
  -#    = link_to " NBA Spread Record", "#nba"
  -#  %h1.secondary-font.center
  -#    Correct: #{@old_games.where(sport: "nba").where(correct_prediction: true).count}
  -#    Incorrect: #{@old_games.where(sport: "nba").where(correct_prediction: false).count}
  -#
  -#%li.list-group-item
  -#  %h2.seconday-font.center
  -#    = link_to " NCAAB Spread Record", "#cbou"
  -#  %h1.secondary-font.center
  -#    Correct: #{@old_games.where(sport: "cb").where(correct_prediction: true).count}
  -#    Incorrect: #{@old_games.where(sport: "cb").where(correct_prediction: false).count}
  -#
  -#%li.list-group-item
  -#  %h2.seconday-font.center
  -#    = link_to " MLB Over/Under Record", "#mlbou"
  -#  %h1.secondary-font.center
  -#    Correct: #{Game.correct_over_under_best_bets("mlb").count}
  -#    Incorrect: #{Game.incorrect_over_under_best_bets("mlb").count}
  -#
  -#%li.list-group-item
  -#  %h2.seconday-font.center
  -#    = link_to " NFL Over/Under Record", "#nflou"
  -#  %h1.secondary-font.center
  -#    Correct: #{@over_under_games.where(sport: "nfl").where(correct_prediction: true).count}
  -#    Incorrect: #{@over_under_games.where(sport: "nfl").where(correct_prediction: false).count}
  -#
  -#%li.list-group-item
  -#  %h2.seconday-font.center
  -#    = link_to " NBA Over/Under Record", "#nbaou"
  -#  %h1.secondary-font.center
  -#    Correct: #{@over_under_games.where(sport: "nba").where(correct_prediction: true).count}
  -#    Incorrect: #{@over_under_games.where(sport: "nba").where(correct_prediction: false).count}
  -#
  -#%li.list-group-item
  -#  %h2.seconday-font.center
  -#    = link_to " NCAAF Over/Under Record", "#cfou"
  -#  %h1.secondary-font.center
  -#    Correct: #{@over_under_games.where(sport: "cf").where(correct_prediction: true).count}
  -#    Incorrect: #{@over_under_games.where(sport: "cf").where(correct_prediction: false).count}
  -#
  -#%li.list-group-item
  -#  %h2.seconday-font.center
  -#    = link_to " NCAAB Over/Under Record", "#cbou"
  -#  %h1.secondary-font.center
  -#    Correct: #{@over_under_games.where(sport: "cb").where(correct_prediction: true).count}
  -#    Incorrect: #{@over_under_games.where(sport: "cb").where(correct_prediction: false).count}
  -#
  -#%li.list-group-item
  -#  %h2.seconday-font.center
  -#    Total Record
  -#  %h1.secondary-font.center
  -#    Correct: #{@old_games.where(correct_prediction: true).count + @over_under_games.where(correct_prediction: true).count}
  -#    Incorrect: #{@old_games.where(correct_prediction: false).count + @over_under_games.where(correct_prediction: false).count}


-#%h1.clear.center.big-font#cb NCAA Basketball Record
-#-@old_games.where(sport: "cb").sort_by{|game| game.strength }.reverse.each do |game|
-#  =render "bet_display", game: game
-#
-#%h1.clear.center.big-font#nfl NFL Record
-#-@old_games.where(sport: "nfl").sort_by{|game| game.strength }.reverse.each do |game|
-#  =render "bet_display", game: game
-#
-#%h1.clear.center.big-font#nba NBA Record
-#-@old_games.where(sport: "nba").sort_by{|game| game.strength }.reverse.each do |game|
-#  =render "bet_display", game: game

-# %h1.clear.center.big-font#mlb College Football Record
-# -Game.best_bets("cf").game_over.where.not("strength > ?", 10000).sort_by{|game| game.strength }.reverse.each do |game|
-#   =render "bet_display", game: game
-# 
-#%h1.clear.center.big-font.padding-top-5#cfou NCAA OU Football Record
-#-@over_under_games.where(sport: "cf").sort_by{|game| game.strength }.reverse.each do |game|
-#  =render "bet_display", game: game
-#
-#%h1.clear.center.big-font#cbou NCAA OU Basketball Record
-#-@over_under_games.where(sport: "cb").sort_by{|game| game.strength }.reverse.each do |game|
-#  =render "bet_display", game: game
-#
-#%h1.clear.center.big-font#nflou NFL OU Record
-#-@over_under_games.where(sport: "nfl").sort_by{|game| game.strength }.reverse.each do |game|
-#  =render "bet_display", game: game
-#
-#%h1.clear.center.big-font#nbaou NBA OU Record
-#-@over_under_games.where(sport: "nba").sort_by{|game| game.strength }.reverse.each do |game|
-#  =render "bet_display", game: game
-#
-#%h1.clear.center.big-font#mlbou MLB OU Record
-#-Game.ou_best_bets("mlb").sort_by{|game| game.strength }.reverse.each do |game|
-#  =render "bet_display", game: game

-# .modal.fade.bd-example-modal-lg{"aria-hidden" => "true", "aria-labelledby" => "myLargeModalLabel", :role => "dialog", :tabindex => "-1"}
-#   .modal-dialog.modal-lg
-#     .modal-content
-#       .jumbotron To filter by 'Strength' enter a number here to only show results of games that scored above that number
